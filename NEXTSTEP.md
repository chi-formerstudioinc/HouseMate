# Next Step

## Where we are

Foundation is done and merged to `main`. The app has all models, services, auth, onboarding, and a 4-tab navigation skeleton. 43 unit tests passing.

**Chapter 2 is the Tasks tab** — the full task list with sorting, filtering, swipe actions, add/edit form, recurring task support, and a template browser.

## When you come back

**1. Create the feature branch:**
```bash
cd /Users/chilee-old/Documents/Development/augment-projects/HouseMate
git checkout -b feature/tasks
git push -u origin feature/tasks
```

**2. Tell Claude:**
> "I'm ready to start the tasks feature on the HouseMate project."

Claude will pick up from `docs/superpowers/plans/2026-03-12-housemate-tasks.md` and walk you through it step by step.

That's it.
